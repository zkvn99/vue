// 아래는 예시로 넣는 최소 설정이며, 실제 프로젝트에서는 필요한 경우에만 수정해서 사용한다.

// Vitest 실행 시 너무 많은 경고가 출력될 경우,
// 테스트 로그 가독성을 높이기 위한 선택적 설정이다.
// (중요한 오류를 가릴 수 있으므로 실제 개발 시 주의해서 사용)
console.warn = (...args) => {
  console.log('[Vitest warning]', ...args);
};